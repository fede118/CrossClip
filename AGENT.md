**Important Note for AI Assistants:**

This is a Kotlin Multiplatform (KMP) project. Please ensure that your responses, suggestions, and code examples are compatible with the KMP paradigm.

Be mindful of KMP-specific limitations. For example:
*   Avoid using `dynamic` or `asDynamic()` for Kotlin/JS, as these are not standard in KMP.
*   Directly embedding JavaScript code using `js("...")` might require specific KMP configurations or workarounds.


Please provide solutions that are idiomatic and work within the constraints of a Kotlin Multiplatform environment.